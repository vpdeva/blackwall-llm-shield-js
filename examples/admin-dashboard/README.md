# Admin Dashboard Example

This is a tiny static example showing how Blackwall Shield event summaries can be rendered for admins or security teams.

Hook it up to your backend endpoint that returns the output of `buildAdminDashboardModel(...)`.

